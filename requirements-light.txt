streamlit==1.38.0
scikit-learn>=1.5.1
numpy==2.2.4
pandas==2.2.2
PyMuPDF==1.24.9
anthropic>=0.30.0
pytest
pytest-cov

# NOTE: This lightweight file excludes heavy ML libraries (torch, sentence-transformers).
# If you need local semantic embeddings, install the full requirements.txt instead.
